const assert = require('assert');
const sequelize = require('../mysql/ot-app-mysql');
describe('test-mysql', () => {
  describe('test user mysql connection', () => {
    it('connection', async () => {
      // var isSuccess = false;
      // await sequelize
      //   .authenticate()
      //   .then(function (success) {
      //     console.log('Connection has been established successfully.');
      //     isSuccess = true;
      //   }, function (errr) {

      //   })
      //   .catch(function (err) {
      //     console.log('Unable to connect to the database:', err);
      //     isSuccess = false;
      //   });

      // assert.strictEqual(isSuccess, true);
    }
    );
  });
});
